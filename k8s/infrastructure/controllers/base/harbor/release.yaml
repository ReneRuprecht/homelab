apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: harbor
  namespace: harbor
spec:
  chart:
    spec:
      chart: harbor
      version: "v1.19.1"
      interval: 12h
      sourceRef:
        kind: HelmRepository
        name: harbor
        namespace: harbor
  interval: 10m
